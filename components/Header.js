import React, {Component} from 'react'

const header = {
  height: '80px'
}

// 头部组件
export default class Header extends Component {
  render() {
    return (
      <div style={header}>
        Header
      </div>
    )
  }
}